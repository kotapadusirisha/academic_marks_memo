# academic_marks_memo
This Python project creates a personalized study memo and opens it in Notepad. It uses basic concepts like file handling, string formatting, and system commands with `os` and `time` modules. It's perfect for beginners to learn simple automation and text file operations.
